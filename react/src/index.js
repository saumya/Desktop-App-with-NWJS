//ReactJS application Entry
import React from 'react'
import ReactDOM from 'react-dom'

import AppContainer from './container/App.container'

ReactDOM.render(
    <AppContainer />,
    document.getElementById('react-app')
);
